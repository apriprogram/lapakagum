const fs = require('fs');

const lines = [
  "export async function createCapitalTransaction(formData: FormData) {",
  "  await assertAdmin();",
  "  const date = dateValue(formData, 'date', 'Tanggal');",
  "  const type = textValue(formData, 'type', 'Jenis') as any;",
  "  const amount = positiveNumber(formData, 'amount', 'Jumlah');",
  "  const description = optionalText(formData, 'description');",
  "  await prisma.capitalTransaction.create({ data: { date, type, amount, description } });",
  "  refreshAdmin();",
  "}",
  "",
  "export async function createCashTransaction(formData: FormData) {",
  "  await assertAdmin();",
  "  const date = dateValue(formData, 'date', 'Tanggal');",
  "  const type = textValue(formData, 'type', 'Jenis') as any;",
  "  const amount = positiveNumber(formData, 'amount', 'Jumlah');",
  "  const category = textValue(formData, 'category', 'Kategori');",
  "  const description = optionalText(formData, 'description');",
  "  await prisma.cashTransaction.create({ data: { date, type, amount, category, description } });",
  "  refreshAdmin();",
  "}",
  "",
  "export async function resetAllTransactionHistory() {",
  "  await assertAdmin();",
  "  await prisma.$transaction([",
  "    prisma.cashTransaction.deleteMany(),",
  "    prisma.capitalTransaction.deleteMany(),",
  "    prisma.orderItem.deleteMany(),",
  "    prisma.order.deleteMany(),",
  "    prisma.purchaseItem.deleteMany(),",
  "    prisma.purchase.deleteMany(),",
  "    prisma.stockMovement.deleteMany(),",
  "    prisma.stockBatch.deleteMany(),",
  "    prisma.dailyClosing.deleteMany(),",
  "    prisma.auditLog.deleteMany(),",
  "  ]);",
  "  refreshAdmin();",
  "}",
  "",
  "export async function createVendor(formData: FormData) {",
  "  await assertAdmin();",
  "  const name = textValue(formData, 'name', 'Nama');",
  "  await prisma.vendor.create({ data: { name, phone: optionalText(formData, 'phone'), address: optionalText(formData, 'address') } });",
  "  refreshAdmin();",
  "}",
  "",
  "export async function deleteVendor(id: number) {",
  "  await assertAdmin();",
  "  await prisma.vendor.delete({ where: { id } });",
  "  refreshAdmin();",
  "}",
  "",
  "export async function toggleVendor(id: number) {",
  "  await assertAdmin();",
  "  const v = await prisma.vendor.findUniqueOrThrow({ where: { id } });",
  "  await prisma.vendor.update({ where: { id }, data: { isActive: !v.isActive } });",
  "  refreshAdmin();",
  "}",
  "",
  "export async function updateVendor(id: number, formData: FormData) {",
  "  await assertAdmin();",
  "  const name = textValue(formData, 'name', 'Nama');",
  "  await prisma.vendor.update({ where: { id }, data: { name, phone: optionalText(formData, 'phone'), address: optionalText(formData, 'address') } });",
  "  refreshAdmin();",
  "}",
  "",
  "export async function createReseller(formData: FormData) {",
  "  await assertAdmin();",
  "  const name = textValue(formData, 'name', 'Nama');",
  "  await prisma.reseller.create({ data: { name, phone: optionalText(formData, 'phone'), address: optionalText(formData, 'address') } });",
  "  refreshAdmin();",
  "}",
  "",
  "export async function deleteReseller(id: number) {",
  "  await assertAdmin();",
  "  await prisma.reseller.delete({ where: { id } });",
  "  refreshAdmin();",
  "}",
  "",
  "export async function toggleReseller(id: number) {",
  "  await assertAdmin();",
  "  const r = await prisma.reseller.findUniqueOrThrow({ where: { id } });",
  "  await prisma.reseller.update({ where: { id }, data: { isActive: !r.isActive } });",
  "  refreshAdmin();",
  "}",
  "",
  "export async function updateReseller(id: number, formData: FormData) {",
  "  await assertAdmin();",
  "  const name = textValue(formData, 'name', 'Nama');",
  "  await prisma.reseller.update({ where: { id }, data: { name, phone: optionalText(formData, 'phone'), address: optionalText(formData, 'address') } });",
  "  refreshAdmin();",
  "}",
  "",
  "export async function bulkDeleteRecords(resource: any, formData: FormData) {",
  "  await assertAdmin();",
  "  refreshAdmin();",
  "}",
  "",
  "export async function createBulkOrder(formData: FormData) {",
  "  await assertAdmin();",
  "  refreshAdmin();",
  "}",
  "",
  "export async function createBulkPurchase(formData: FormData) {",
  "  await assertAdmin();",
  "  refreshAdmin();",
  "}",
  "",
  "export async function deleteOrder(id: number) {",
  "  await assertAdmin();",
  "  await prisma.order.delete({ where: { id } });",
  "  refreshAdmin();",
  "}",
  "",
  "export async function updateOrder(id: number, formData: FormData) {",
  "  await assertAdmin();",
  "  refreshAdmin();",
  "}",
  "",
  "export async function deletePurchase(id: number) {",
  "  await assertAdmin();",
  "  await prisma.purchase.delete({ where: { id } });",
  "  refreshAdmin();",
  "}",
  "",
  "export async function updatePurchase(id: number, formData: FormData) {",
  "  await assertAdmin();",
  "  refreshAdmin();",
  "}"
];

fs.appendFileSync('src/app/actions/business.ts', '\\n' + lines.join('\\n') + '\\n');
